# Cloudflare Pages 快取策略（static-asset-cache-policy）。
# 規則：沒有 content-hash 的檔案用 no-cache —— 瀏覽器「存下 body 但每次用 ETag 重新驗證」，
# 未變回 304（零 body、便宜），改版即拿到新檔。絕不用 no-store（那才會每次整包重載）。
# Cloudflare Pages 會自動產生 ETag 並回應條件式請求（304），所以 no-cache 有 validator 可依靠。

# 預設：會隨改版變動的檔（HTML / JS / CSS / 資料 JSON）→ 永遠重新驗證、永遠最新樣式與程式碼。
/*
  Cache-Control: no-cache

# 穩定的本地 SVG 圖示（屬性 / UI icon）幾乎不變 → 快取 30 天，省掉每次造訪的驗證往返。
# 若日後改了某個 icon，最久 30 天內自行更新（非 immutable，可自癒）。
/assets/*
  Cache-Control: public, max-age=2592000

# 註：寶可夢立繪是外部 PokéAPI / GitHub CDN（raw.githubusercontent.com），由上游長快取，
# 不經本站，無需在此設定。
